server:
  port: 8080
spring:
  datasource:
    username: root
    password: Xixi@nd11
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/ape-frame?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 20
      min-idle: 20
      max-active: 100
      max-wait: 60000
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        login-username: root
        login-password: Xixi@nd11
      filter:
        stat:
          enabled: true
          log-slow-sql: true
          slow-sql-millis: 2000
        wall:
          enabled: true
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0
    lettuce:
      pool:
        max-active: 20
        max-idle: 8
        max-wait: -1
        min-idle: 0
    password: hibiki
sql:
  beauty:
    show: false
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: delete_flag
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-location: classpath:mapper/*.xml
swagger:
  base-package: com.jingdianjichi
  title: ape-frame
  contact-name: 经典鸡翅
  contact-url: dasd
  contact-email: dsa
  version: 1.0
  description: 从零到一开箱即用脚手架
logging:
  config: classpath:log4j2-spring.xml
log:
  aspect:
    enable: false
guava:
  cache:
    switch: true


